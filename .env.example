# Base de datos PostgreSQL (requerida por Prisma CLI y el seed)
DATABASE_URL="postgresql://postgres:TU_PASSWORD@localhost:5432/control_gastos?schema=public"

# URL de la API (backend Express)
API_URL="http://localhost:4000"

# Secreto JWT (debe coincidir con backend/.env)
# Genera uno con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="GENERA_UN_SECRETO_AQUI"